<?xml version="1.0" encoding="utf-8"?>
<!--
  Quick-add button. A filled pill in the accent, matching the in-app amount
  buttons, so the one tappable thing on the widget looks tappable.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/widget_aqua" />
    <corners android:radius="@dimen/widget_button_radius" />
</shape>
