<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Show notifications on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Precise, time-critical reminders -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- Re-schedule pending reminders after the device reboots -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Drink Water"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- flutter_local_notifications: delivers scheduled alarms while the app
             process is not running. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <!-- Restores all scheduled reminders after reboot / app update, so the
             user never silently stops getting notified. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Handles the "Drank it" / "Taken" notification action buttons without
             opening the UI. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>

        <!-- Home screen widgets. Both providers must be exported: the launcher and
             the system send APPWIDGET_UPDATE from outside this app, and an
             unexported receiver would simply never be updated. Because exported
             also means "any app can send this anything", the widget buttons go to
             the unexported WidgetActionReceiver below instead.

             Both widgets show "today", so they are wrong the instant the date
             changes. TIME_SET and TIMEZONE_CHANGED cover a user changing the clock.
             DATE_CHANGED is listed for completeness but should not be relied on: it
             is not on Android 8's implicit-broadcast exemption list, so a
             manifest-declared receiver very likely never sees it. The backstop for
             the ordinary midnight rollover is updatePeriodMillis, which means the
             totals can read up to half an hour stale just after midnight. -->
        <receiver
            android:name=".WaterWidgetProvider"
            android:label="@string/widget_water_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="android.intent.action.DATE_CHANGED"/>
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_water"/>
        </receiver>

        <receiver
            android:name=".MedicineWidgetProvider"
            android:label="@string/widget_medicine_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
                <action android:name="android.intent.action.DATE_CHANGED"/>
                <action android:name="android.intent.action.TIME_SET"/>
                <action android:name="android.intent.action.TIMEZONE_CHANGED"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/appwidget_medicine"/>
        </receiver>

        <!-- The widget buttons. Not exported and with no intent-filter: it is only
             ever reached by an explicit PendingIntent this app built, which is
             exactly what keeps another app from logging a drink or a dose. -->
        <receiver
            android:name=".WidgetActionReceiver"
            android:exported="false"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
